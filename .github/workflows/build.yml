---
name: Build Packages

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

env:
  APP_NAME: kiru

jobs:
  build-deb:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v4

    - name: Generate version
      run: |
        VERSION=${{ github.ref_name }}
        VERSION=${VERSION#v}  # Strip leading 'v'
        echo $VERSION > VERSION
        echo "APP_VERSION=$VERSION" >> $GITHUB_ENV

    - name: Cache lightweight dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache
        key: ${{ runner.os }}-deb-${{ hashFiles('.github/workflows/build.yml') }}

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          gjs \
          libgtk-3-dev \
          libgirepository1.0-dev \
          libsoup2.4-dev \
          libcairo2-dev \
          libpango1.0-dev \
          libgdk-pixbuf-2.0-dev \
          libglib2.0-dev \
          gnome-screenshot \
          curl \
          xclip \
          shared-mime-info \
          imagemagick \
          dpkg-dev

    - name: Validate main.js exists
      run: |
        if [ ! -f "main.js" ]; then
          echo "Error: main.js not found!"
          exit 1
        fi
        if [ ! -f "VERSION" ]; then
          echo "Error: VERSION not found!"
          exit 1
        fi

    - name: Create package structure
      run: |
        mkdir -p ${{ env.APP_NAME }}/DEBIAN
        mkdir -p ${{ env.APP_NAME }}/usr/bin
        mkdir -p ${{ env.APP_NAME }}/usr/share/applications
        mkdir -p ${{ env.APP_NAME }}/usr/share/icons/hicolor/512x512/apps
        mkdir -p ${{ env.APP_NAME }}/usr/share/doc/${{ env.APP_NAME }}

        cp main.js ${{ env.APP_NAME }}/usr/bin/${{ env.APP_NAME }}
        cp screenshot-editor.js ${{ env.APP_NAME }}/usr/bin/
        cp main-app.js ${{ env.APP_NAME }}/usr/bin/
        chmod +x ${{ env.APP_NAME }}/usr/bin/${{ env.APP_NAME }}

        cat > ${{ env.APP_NAME }}/DEBIAN/control << EOF
        Package: ${{ env.APP_NAME }}
        Version: ${{ env.APP_VERSION }}
        Vcs-Browser: https://github.com/${{ github.repository }}
        Section: utils
        Priority: optional
        Architecture: amd64
        Depends: gjs, libgtk-3-0, libgirepository-1.0-1, libsoup2.4-1, libcairo2, libpango-1.0-0, libgdk-pixbuf2.0-0, libglib2.0-0, gnome-screenshot, curl, xclip, shared-mime-info
        Maintainer: Your Name <your.email@example.com>
        Description: Screenshot editor application
         A screenshot editor built with GJS and GTK.
        EOF

        cat > ${{ env.APP_NAME }}/usr/share/applications/${{ env.APP_NAME }}.desktop << EOF
        [Desktop Entry]
        Name=Kiru
        Comment=Screenshot editor
        Exec=${{ env.APP_NAME }}
        Icon=${{ env.APP_NAME }}
        Type=Application
        Categories=Graphics;
        Terminal=false
        EOF

        cat > ${{ env.APP_NAME }}/usr/share/doc/${{ env.APP_NAME }}/copyright << EOF
        Format: https://www.debian.org/doc/packaging-manuals/copyright-format/1.0/
        Upstream-Name: ${{ env.APP_NAME }}
        Source: https://github.com/${{ github.repository }}

        Files: *
        Copyright: $(date +%Y) ${{ github.repository_owner }}
        License: MIT
        EOF

        if [ -f "assets/icon.png" ]; then
          cp assets/icon.png ${{ env.APP_NAME }}/usr/share/icons/hicolor/512x512/apps/${{ env.APP_NAME }}.png
        else
          convert -size 512x512 xc:blue ${{ env.APP_NAME }}/usr/share/icons/hicolor/512x512/apps/${{ env.APP_NAME }}.png 2>/dev/null || \
          touch ${{ env.APP_NAME }}/usr/share/icons/hicolor/512x512/apps/${{ env.APP_NAME }}.png
        fi

    - name: Build DEB package
      run: |
        dpkg-deb --build ${{ env.APP_NAME }}
        mv ${{ env.APP_NAME }}.deb ${{ env.APP_NAME }}-${{ env.APP_VERSION }}-amd64.deb
        dpkg-deb --info ${{ env.APP_NAME }}-${{ env.APP_VERSION }}-amd64.deb
        dpkg-deb --contents ${{ env.APP_NAME }}-${{ env.APP_VERSION }}-amd64.deb

    - name: Upload DEB artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.APP_NAME }}-deb
        path: ${{ env.APP_NAME }}-${{ env.APP_VERSION }}-amd64.deb
        if-no-files-found: error

  build-rpm:
    runs-on: ubuntu-22.04
    container:
      image: fedora:39
    steps:
    - uses: actions/checkout@v4

    - name: Generate version
      run: |
        VERSION=${{ github.ref_name }}
        VERSION=${VERSION#v}  # Strip leading 'v'
        echo $VERSION > VERSION
        echo "APP_VERSION=$VERSION" >> $GITHUB_ENV

    - name: Install dependencies
      run: |
        dnf update -y
        dnf install -y \
          gjs \
          gtk3-devel \
          gobject-introspection-devel \
          libsoup-devel \
          cairo-devel \
          pango-devel \
          gdk-pixbuf2-devel \
          glib2-devel \
          gnome-screenshot \
          curl \
          xclip \
          shared-mime-info \
          rpm-build \
          rpmdevtools

    - name: Setup RPM build environment
      run: |
        rpmdev-setuptree
        cp main.js ~/rpmbuild/SOURCES/
        cp screenshot-editor.js ~/rpmbuild/SOURCES/
        cp main-app.js ~/rpmbuild/SOURCES/
        cat > ~/rpmbuild/SOURCES/${{ env.APP_NAME }}.desktop << EOF
        [Desktop Entry]
        Name=Kiru
        Comment=Screenshot editor
        Exec=${{ env.APP_NAME }}
        Icon=${{ env.APP_NAME }}
        Type=Application
        Categories=Graphics;
        Terminal=false
        EOF
        touch ~/rpmbuild/SOURCES/${{ env.APP_NAME }}.png

    - name: Create RPM spec file
      run: |
        cat > ~/rpmbuild/SPECS/${{ env.APP_NAME }}.spec << 'SPECEOF'
        Name:           kiru
        Version:        %{getenv:APP_VERSION}
        Release:        1%{?dist}
        Summary:        Screenshot editor application
        License:        MIT
        URL:            https://github.com/%{getenv:GITHUB_REPOSITORY}
        BuildArch:      x86_64
        Source0:        main.js
        Source1:        kiru.desktop
        Source2:        kiru.png

        Requires:       gjs gtk3 gobject-introspection libsoup cairo pango gdk-pixbuf2 glib2 gnome-screenshot curl xclip shared-mime-info

        %description
        A screenshot editor built with GJS and GTK.

        %prep
        %build
        %install
        mkdir -p %{buildroot}/usr/bin
        mkdir -p %{buildroot}/usr/share/applications
        mkdir -p %{buildroot}/usr/share/icons/hicolor/512x512/apps
        install -m 0755 %{SOURCE0} %{buildroot}/usr/bin/kiru
        install -m 0644 %{SOURCE1} %{buildroot}/usr/share/applications/kiru.desktop
        install -m 0644 %{SOURCE2} %{buildroot}/usr/share/icons/hicolor/512x512/apps/kiru.png

        %files
        %attr(0755,root,root) /usr/bin/kiru
        /usr/share/applications/kiru.desktop
        /usr/share/icons/hicolor/512x512/apps/kiru.png

        %changelog
        * $(date "+%a %b %d %Y") GitHub Actions <noreply@github.com> - 1.0-1
        - Initial package
        SPECEOF

    - name: Build RPM package
      run: |
        export GITHUB_RUN_NUMBER="${{ github.run_number }}"
        export GITHUB_REPOSITORY="${{ github.repository }}"
        rpmbuild -bb ~/rpmbuild/SPECS/${{ env.APP_NAME }}.spec
        RPM_FILE=$(ls ~/rpmbuild/RPMS/x86_64/${{ env.APP_NAME }}-*.rpm)
        cp "$RPM_FILE" ${{ env.APP_NAME }}-${{ env.APP_VERSION }}-x86_64.rpm
        rpm -qip ${{ env.APP_NAME }}-${{ env.APP_VERSION }}-x86_64.rpm
        rpm -qlp ${{ env.APP_NAME }}-${{ env.APP_VERSION }}-x86_64.rpm

    - name: Upload RPM artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.APP_NAME }}-rpm
        path: ${{ env.APP_NAME }}-${{ env.APP_VERSION }}-x86_64.rpm
        if-no-files-found: error

  build-appimage:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v4

    - name: Generate version
      run: |
        VERSION=${{ github.ref_name }}
        VERSION=${VERSION#v}  # Strip leading 'v'
        echo $VERSION > VERSION
        echo "APP_VERSION=$VERSION" >> $GITHUB_ENV

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          gjs \
          libgtk-3-0 \
          libgirepository-1.0-1 \
          libsoup2.4-1 \
          libcairo2 \
          libpango-1.0-0 \
          libgdk-pixbuf2.0-0 \
          libglib2.0-0 \
          gnome-screenshot \
          curl \
          xclip \
          shared-mime-info \
          imagemagick \
          file \
          desktop-file-utils \
          appstream-util \
          wget

    - name: Download appimagetool
      run: |
        wget -O appimagetool https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage
        chmod +x appimagetool

    - name: Install libfuse2 for appimagetool
      run: sudo apt-get update && sudo apt-get install -y libfuse2

    - name: Create AppDir structure
      run: |
        mkdir -p AppDir/usr/bin
        mkdir -p AppDir/usr/lib
        mkdir -p AppDir/usr/share/applications
        mkdir -p AppDir/usr/share/icons/hicolor/512x512/apps
        cp main.js AppDir/usr/bin/${{ env.APP_NAME }}
        cp screenshot-editor.js AppDir/usr/bin/
        cp main-app.js AppDir/usr/bin/
        chmod +x AppDir/usr/bin/${{ env.APP_NAME }}

        cat > AppDir/usr/share/applications/${{ env.APP_NAME }}.desktop << EOF
        [Desktop Entry]
        Name=Kiru
        Comment=Screenshot editor
        Exec=${{ env.APP_NAME }}
        Icon=${{ env.APP_NAME }}
        Type=Application
        Categories=Graphics;
        Terminal=false
        EOF

        cat > AppDir/AppRun << EOF
        #!/bin/bash
        SELF=$(readlink -f "$0")
        HERE=${SELF%/*}
        export PATH="${HERE}/usr/bin:${PATH}"
        export LD_LIBRARY_PATH="${HERE}/usr/lib:${LD_LIBRARY_PATH}"
        export GI_TYPELIB_PATH="${HERE}/usr/lib/girepository-1.0:${GI_TYPELIB_PATH}"
        exec "${HERE}/usr/bin/${{ env.APP_NAME }}" "$@"
        EOF
        chmod +x AppDir/AppRun

        if [ -f "assets/icon.png" ]; then
          cp assets/icon.png AppDir/usr/share/icons/hicolor/512x512/apps/${{ env.APP_NAME }}.png
          cp assets/icon.png AppDir/${{ env.APP_NAME }}.png
        else
          touch AppDir/usr/share/icons/hicolor/512x512/apps/${{ env.APP_NAME }}.png
          touch AppDir/${{ env.APP_NAME }}.png
        fi
        ln -sf usr/share/applications/${{ env.APP_NAME }}.desktop AppDir/${{ env.APP_NAME }}.desktop

    - name: Build AppImage
      run: |
        ARCH=x86_64 ./appimagetool --comp gzip AppDir ${{ env.APP_NAME }}-${{ env.APP_VERSION }}-x86_64.AppImage
        chmod +x ${{ env.APP_NAME }}-${{ env.APP_VERSION }}-x86_64.AppImage

    - name: Validate AppImage (extraction only, no FUSE)
      run: |
        ./${{ env.APP_NAME }}-${{ env.APP_VERSION }}-x86_64.AppImage --appimage-extract
        test -f squashfs-root/usr/bin/${{ env.APP_NAME }} && echo "AppImage structure OK"

    - name: Upload AppImage artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.APP_NAME }}-appimage
        path: ${{ env.APP_NAME }}-${{ env.APP_VERSION }}-x86_64.AppImage
        if-no-files-found: error

  create-release:
    needs: [build-deb, build-rpm, build-appimage]
    runs-on: ubuntu-22.04
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Download all artifacts
      uses: actions/download-artifact@v4

    - name: Generate release notes
      id: release_notes
      run: |
        LAST_TAG=$(git tag --sort=-committerdate | sed -n '2p' || git rev-list --max-parents=0 HEAD)
        echo "## What's Changed" > release_notes.md
        echo "" >> release_notes.md
        git log ${LAST_TAG}..HEAD --pretty=format:"- %s (%h)" --no-merges >> release_notes.md
        echo "" >> release_notes.md
        echo "" >> release_notes.md
        echo "## Package Downloads" >> release_notes.md
        echo "" >> release_notes.md
        echo "### Debian/Ubuntu (.deb)" >> release_notes.md
        echo '```bash' >> release_notes.md
        echo "sudo dpkg -i kiru-${{ env.APP_VERSION }}-amd64.deb" >> release_notes.md
        echo "sudo apt-get install -f  # Fix dependencies" >> release_notes.md
        echo '```' >> release_notes.md
        echo "" >> release_notes.md
        echo "### Fedora/RHEL (.rpm)" >> release_notes.md
        echo '```bash' >> release_notes.md
        echo "sudo dnf install kiru-${{ env.APP_VERSION }}-x86_64.rpm" >> release_notes.md
        echo '```' >> release_notes.md
        echo "" >> release_notes.md
        echo "### Universal (AppImage)" >> release_notes.md
        echo '```bash' >> release_notes.md
        echo "chmod +x kiru-${{ env.APP_VERSION }}-x86_64.AppImage" >> release_notes.md
        echo "./kiru-${{ env.APP_VERSION }}-x86_64.AppImage" >> release_notes.md
        echo '```' >> release_notes.md
        echo "" >> release_notes.md
        echo "**Full Changelog**: https://github.com/${{ github.repository }}/compare/${LAST_TAG}...v${{ env.APP_VERSION }}" >> release_notes.md

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: v${{ env.APP_VERSION }}
        name: Release v${{ env.APP_VERSION }}
        body_path: release_notes.md
        draft: false
        prerelease: false
        files: |
          ${{ env.APP_NAME }}-deb/*.deb
          ${{ env.APP_NAME }}-rpm/*.rpm
          ${{ env.APP_NAME }}-appimage/*.AppImage
        generate_release_notes: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Log release URL
      run: echo "Release v${{ env.APP_VERSION }} created at https://github.com/${{ github.repository }}/releases/tag/v${{ env.APP_VERSION }}"


